# SimpleSpy - Перехватчик Remote с автосохранением

## 📋 Описание

SimpleSpy - это мощный инструмент для перехвата и логирования всех Remote событий в Roblox играх. Скрипт автоматически сохраняет все перехваченные Remote на рабочий стол в структурированном виде.

## ✨ Основные возможности

### 🎯 Перехват Remote
- **RemoteEvent** - перехват всех вызовов `FireServer`
- **RemoteFunction** - перехват всех вызовов `InvokeServer`
- Автоматическое обнаружение новых Remote при их создании
- Полное отображение всех аргументов с типами данных

### 💾 Автосохранение на рабочий стол
Скрипт автоматически создает следующую структуру папок:
```
Рабочий стол/
└── RemoteSpy/
    └── [Название игры]/
        ├── RemoteEvent1.txt
        ├── RemoteEvent2.txt
        └── RemoteFunction1.txt
```

**Особенности сохранения:**
- Каждый Remote сохраняется в отдельный файл с его названием
- Каждая запись разделена 2-3 пустыми строками для удобства чтения
- Автоматическое добавление временных меток
- Если папка с названием игры уже существует, файлы добавляются в нее
- Новые записи добавляются в конец существующих файлов

### 🎨 Интерфейс (100% на русском)

#### Главные кнопки:
1. **Очистить** - очищает все логи из интерфейса
2. **Сохранить все** - принудительно сохраняет все текущие логи на рабочий стол
3. **Пауза** - приостанавливает перехват новых Remote
4. **Копировать** - копирует все логи в буфер обмена

#### 🔐 Супер секретная кнопка "🔒 VIP"
**Что это такое:**
Это специальная кнопка, которая активирует VIP режим с продвинутыми функциями:
- **Авто-генерация скриптов** - автоматическое создание Lua кода для воспроизведения перехваченных Remote
- **Продвинутые фильтры** - фильтрация Remote по типу, названию, времени
- **Экспорт в JSON** - экспорт данных в JSON формат для дальнейшей обработки
- **Продвинутый поиск** - быстрый поиск по логам с поддержкой регулярных выражений

При нажатии кнопка меняется на "🔓 VIP" и показывает, что режим активирован.

## 📝 Формат сохранения

Каждая запись в файле имеет следующий формат:
```
[2024-01-14 15:30:45] ReplicatedStorage.Remotes.CoinRemote (RemoteEvent)
[1] Player: Players.YourName
[2] Coins: 100
[3] Action: "collect"


```

## 🚀 Установка и использование

1. Запустите ваш любимый executor для Roblox
2. Загрузите скрипт `SimpleSpySource.lua`
3. Выполните скрипт
4. Откроется GUI интерфейс SimpleSpy
5. Все Remote будут автоматически перехватываться и сохраняться

## 📊 Информационная панель

В нижней части окна отображается:
- 📁 Путь сохранения файлов
- 📈 Количество перехваченных логов
- ✅ Статусы операций (сохранение, копирование)

## 🎯 Примеры использования

### Поиск эксплойтов
Используйте SimpleSpy для анализа Remote вызовов и поиска уязвимостей в играх.

### Создание автоматизации
Перехваченные Remote можно использовать для создания автоматических скриптов.

### Отладка
Разработчики могут использовать для отладки и анализа работы Remote в своих играх.

## ⚙️ Настройки

Вы можете изменить настройки в начале скрипта:
```lua
local Settings = {
    AutoSave = true,        -- Автосохранение на рабочий стол
    MaxLogs = 1000,         -- Максимальное количество логов в памяти
    ShowTimestamp = true,   -- Показывать временные метки
    SaveToDesktop = true,   -- Сохранять на рабочий стол
}
```

## 🔧 Технические детали

### Совместимость
- ✅ Synapse X
- ✅ Script-Ware
- ✅ KRNL
- ✅ Fluxus
- ✅ Большинство современных executors

### Требования
- Executor с поддержкой:
  - `makefolder()` - для создания папок
  - `writefile()` / `readfile()` / `isfile()` - для работы с файлами
  - `setclipboard()` - для копирования в буфер обмена (опционально)

### Защита
Скрипт использует:
- `gethui()` для защиты GUI от обнаружения
- `syn.protect_gui()` для дополнительной защиты
- Обработку ошибок через `pcall()` для стабильности

## 🐛 Решение проблем

**Файлы не сохраняются:**
- Убедитесь, что ваш executor поддерживает функции `writefile()` и `makefolder()`
- Проверьте, что у вас есть права на запись на рабочий стол

**GUI не отображается:**
- Убедитесь, что скрипт полностью загрузился
- Проверьте консоль на наличие ошибок

**Не все Remote перехватываются:**
- Некоторые игры используют защиту от перехвата
- Попробуйте обновить executor до последней версии

## 📜 Лицензия

Этот скрипт создан исключительно в образовательных целях. Используйте на свой страх и риск.

## 👨‍💻 Автор

SimpleSpy 2.0 с функцией автосохранения - улучшенная версия с полной локализацией на русский язык.

---

**Версия:** 2.0
**Дата обновления:** 2024-01-14
**Статус:** Стабильная версия
